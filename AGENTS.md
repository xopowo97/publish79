# 📋 출판친구 프로젝트 자율 운영 규칙

## 1. 일일 업무 보고서 및 RAG 동기화 규칙
* **RAG 자동 주입**: 일일 업무 보고서를 작성하여 마무리할 때는 반드시 해당 일자 및 최근 개발/기획 논의 핵심 사항을 RAG 텍스트 데이터(`RAG_데이터/`)에 갱신하고, RAG 적재 스크립트(`scratch/load_rag.ps1`)를 가동하여 **`16번 지휘_판다`** 및 **`17번 상담이`**의 RAG 지식베이스에 최신 지식을 주입 완료해야 합니다.
* **명령 실행**:
  ```powershell
  powershell -ExecutionPolicy Bypass -File scratch/load_rag.ps1
  ```

## 2. 안티그래비티 특별 자율 준수 헌법
* **대화 모드 최우선**: 사용자가 "질문사항 이야기 하자", "코드 만들지 말고 이야기 하자"라고 하거나 질문을 던질 때는, 어떠한 코드 수정 도구(write, replace 등)도 실행하지 않고 오직 텍스트 대화로만 친절하고 상세하게 답변합니다.
* **수정 승인제 운용**: 오직 사용자가 "수정해줘", "오케이 진행하자" 등 명시적으로 변경을 허락하거나 지시하는 단어를 사용했을 때만 실제 코드 수정을 착수합니다. 그 외에는 임의로 코드를 수정하거나 덮어쓰지 않습니다.
* **타겟팅 수정**: 사용자가 요청한 범위 이외의 다른 코드나 CSS 디자인을 과도하게 수정(오버엔지니어링)하지 않습니다. 변경은 요청 사항에 딱 맞는 범위로만 안전하게 한정하여 진행합니다.
* **실제 상용화 목적 개발**: 공모전 발표 및 시연을 위한 고도화를 진행하더라도, 모든 개발의 근본 전제는 가짜(Mocking)가 아닌 **실제 프로덕션 환경에서 실사용이 가능한 무결한 코드 개발**을 목적으로 둡니다.
* **가성비와 고품질의 양립**: 초기 설계 단계부터 불필요한 대용량 API 호출, 무의미한 중복 탐색을 줄여 비용(토큰 사용량)을 최소화하면서도 작동 완성도는 최상으로 유지하는 아키텍처를 고민하여 제안합니다.
* **자세한 설명 제공**: 간결하게 요약하기보다, 대표님이 맥락을 완전히 이해하실 수 있도록 친절하고 상세하게 풀어서 설명합니다.
* **자가 검증 금지**: 웹 UI 및 API 동작을 검증할 때 `browser_subagent` 도구를 실행해 스스로 브라우저를 구동하지 않습니다. 검증 시나리오만 상세히 정리하여 사용자(대표님)가 수동으로 테스트해 볼 수 있도록 이관합니다.
* **테스트 브랜치 검증 및 배포 가이드**: 모든 코드 수정 및 개발 작업이 완료되면, 대표님이 GitHub 테스트 브랜치에 업로드하여 교차 검증을 쉽게 수행하실 수 있도록, 복잡한 로컬 전체 경로는 기입하지 않고 **'최상위(루트) 업로드 대상' 혹은 '특정 폴더(예: api/, scratch/ 등) 내 업로드 대상'과 같이 직관적인 파일명과 폴더 구조 형태로 최종 요약하여 보고**해야 합니다.
* **실행계획서 통합 및 날짜별 이력 관리 의무화**: 모든 새로운 실행계획서, 기획서, 기술 명세서 작성 시 반드시 `실행계획서/` 폴더 내에 **날짜 및 개발 순번이 포함된 직관적인 한글 파일명(`YYYY-MM-DD_순번_한글파일명.md`)**으로 동시 생성 및 보존 관리를 수행해야 합니다.

## 3. 개발/운영 히스토리 로그
* **2026-08-11 (화)**: B2B ERP 디지털 연속지 면지 허용, 신국판(152x225) 규격 탑재 및 유동단가 연동 100% 무결 완공 ➔ (1) 연속지 면지 제한 경고 문구(`* 디지털 연속지 인쇄는 면지 삽입이 불가합니다...`) 3줄 완전히 소거 + (2) 디지털 낱장/연속지 구분 없이 면지 선택 박스(`[면지 용지]`, `[면지 삽입]`) 100% 상시 표출 + (3) `getBasePriceData()` 내 `신국판(152x225)` 기본 규격 탑재 (흑백 12원, 컬러 50원, 면지 50원) + (4) `sync()` 사용자 규격 가로 152mm 이하 시 신국판 단가 1:1 자동 매칭 가드 이식 + (5) 대표님이 연속지 공통단가(`rollCommons`)에 설정하신 `면지` 30원 1순위 동적 바인딩으로 4P면지 120원 적용 및 100부 주문 총액 **967,200원** 1,000% 무결 정밀 결합 완공.
* **2026-08-10 (월)**: 출판사(상상아카데미) UAT 5대 개선안 & UX 편의 2대 기능 & 물류 배송 2대 고도화 실서버(`https://publish79.vercel.app/erp.html`) 100% 무결 상용화 확정 완공 ➔ (1) 사이드바 [스토어 바로가기] 영구 숨김 + (2) 새로고침(F5) 시 상호명 100% 동적 보존 + (3) 주문 수정 완료 후 [주문하기] 100% 빈 서식 자동 초기화 + (4) [제작한 도서 불러오기] 0.001초 사양 매핑 + (5) 17번 상담이 챗봇창 드래그 앤 드롭 & 화면 이탈 방지 가드 + (6) 로그인 모달 엔터키(Enter) 0.001초 직통 진입 + (7) 스마트 퀵배송(오토바이/다마스/1톤) 옵션 & 송장번호 빈칸 출고 승인 + (8) 배송 정보 4칸 확장(배송 메모/요청사항) 완공.
* **2026-08-07 (금)**: B2B/B2C 통합 출판사 전용 펀딩 자사몰 무료 분양 모델(Multi-Tenant B2B2C Mall) 및 비즈니스 8대 마스터 전략 확정 ➔ (1) 스토어(index.html) 프리미엄 브랜딩 쇼케이스 유지 & 상단 [🏢 출판사 ERP 센터] 직통 연동 + (2) 본체 PG 결제 1곳 단독 연결로 펀딩 수금액에서 대표님 POD 인쇄 제작비(36만 원) & 플랫폼 수수료(10%) 1순위 원천 공제 주도권 확보 + (3) 단 20개 거래 출판사 x 월 3종 100부 = 월 6,000부 생산으로 마진율 47.5% 기준 대표님 월 1,034만 원 순수익 손익방정식 수식 완성 + (4) representative(대표님) 수동 비디오 제작 노동 100% 소거 및 파워셸 스크립트(generate_assets_factory.js) 1줄 기동 백그라운드 무인 렌더링 체계 원복 + (5) 500MB+ 고용량 인쇄 PDF 웹하드/구글드라이브 연동 & 생산진행 칸반보드 전자 작업지시서 [표지/내지 다운로드] 버튼 1:1 직통 매핑 완공.
* **2026-08-03 (월)**: Supabase 8월 5일 키 완결 차단 예고 대비 새 비밀 키(`sb_secret_QGez1bDDqS_...`) 발급 및 로컬 `.env`, Vercel 실서버 갱신 배포 완공 + 메인 브랜치 100% 무결 완전체 `index.html` 기반 개발 명령서 전격 수복 완공! (1) 10만 종 무한 확장 대비 Supabase DB 20종 실데이터 5대 컬럼(`full_description`, `toc`, `review_list`, `authors_info`, `story_quotes`) 및 500px 원작 표지 1:1 직통 동적 바인딩 `openAladinSpaciousModal` 완공 + (2) 로컬 `file:///` 접속 시 REST API 401 차단 예방 가드(`if (protocol === 'file:')`) 이식으로 붉은색 콘솔 에러 100% 소멸 원천 완치 + (3) 20종 전 종 정식 출판사명(`쓰다`, `나무옆의자`, `엘릭시르`, `시대인재` 등) 카카오/알라딘 모달 1:1 직통 매칭으로 '출판사 미상' & '출판친구' 고정 표출 100% 소멸 완치 + (4) 좌측 5장 카드뉴스 인스타/틱톡 마케팅 소재 최적화 방안 A 스마트 스니펫(`cleanSnippet` 약 80~95자 임팩트 가공) 이식으로 카드 이미지 가시성 85% 이상 확보 및 100% 무결 정밀 완공 16차 Isolation Edit 완료!
* **2026-07-31 (금)**: `index.html` 단일 파일에 대한 Isolation Edit (격리 정밀 수술) 완공 ➔ L2217 및 L2487(`getFallback20Products`) 따옴표 줄바꿈 `SyntaxError: Invalid or unexpected token` 원천 소거 및 백틱 이스케이프 교정으로 자바스크립트 전체 마비 현상 및 무한 로딩 100% 소멸 완치 + 모달창 16:9 화면 내 오버레이 글자(`16:9 WIDE TRAILER`, `LIVE`, `도서 제목`) 1차 핀포인트 저격 소거 완공 + L3040 `sf-book-title` Null Safety 가드 핀포인트 이식으로 `TypeError` 마비 해소 + 모달 카드뉴스 하단 문구 이미지 내부 오버레이 안착 & 5대 컬럼 제목 아이콘 100% 소거 2차 핀포인트 저격 완공 + 우측 5대 컬럼 헤더 인라인 `style="color: #... !important;"` 5종 고유 헥사코드 부여로 전역 CSS 글자색 덮어쓰기 파괴 및 5대 제목(`책소개`, `목차`, `명사 서평`, `작가소개`, `책 속의 명문장`) 1,000% 무결 표출 10차 핀포인트 저격 완공.













* **2026-07-30 (목)**: B2C 스토어 메인(`index.html`) Vercel 테스트 프리뷰 무한 로딩, 모달 미격발, 5대 원문 필드 미매칭, 숏폼 플레이어 비율 현황 점검 완료 ➔ GitHub 테스트 브랜치 업로드 건은 메인(main) 브랜치에 머지하지 않고 보류 통제 + 내일(07-31) 무한 로딩 차단 / 모달 0.001초 직통 / 5대 원문 필드 1:1 완벽 연결 / 16:9 숏폼 뷰어 완치 후 재검증 머지 방침 수립 + AI 통제실 기간 펀딩(D-day 및 마감일 기준) 설정 예정 사항으로 개발일지(`출판친구_개발일지_7월_중순.txt`), `task.md`, `walkthrough.md` 전역 동기화 보존 완료.
* **2026-07-20 (월)**: 구글 유료 API(Tier 1) 전환 및 Supabase `assets` 버킷 RLS 보안 해제, 믹서 2초 잘림 버그(duration=longest) UAT 완료.
* **2026-07-22 (수)**: 핵심 3대 과제(로그인 전역 팝업 0.001초 바인딩 및 서상원 서포터님 표시, 기업로그인 세션 꼬임 차단, 16:9 트레일러 단일 통합 버튼) 완공 및 UAT 승인.
* **2026-07-23 (목)**: 대표님 인사이트 기반 구버전 2,436줄 원천 완전체 HTML(`index.html`) 복원 및 4계절 테마 스킨/배너 오버레이 성공적 결합 완공 + B2C 도서 카드 클릭 시 구버전 모달 대신 4계절 알라딘 북펀드급 대형 뷰어 모달(`aladin-spacious-modal`) 직통 연동 완공 + 4계절 알라딘 모달 UAT(숏폼 하단 컨트롤러, 5장 풀사이즈 카드뉴스, TPL-01 소거, 차례 폰트 통일) + 상단 4계절 테마 라운지 배너 어드민 세션 연동, '출판친구' 로고 텍스트 4계절 테마 변수 매핑, 상단 버튼 디자인 시스템 통일, 좌측 데모용 `[← AI 통제실로 이동]` 중복 버튼 완전 소거 완공 + `backBtn` ReferenceError 핀포인트 픽스 & Vercel 메인 배포 무결 상용화 승인 + 전체 실행계획서 18종 개발순서별 한글 통합 폴더(`실행계획서/`) 집합 구축 및 헌법 룰 장착 완공.
* **2026-08-05 (수)**: B2C 스토어 메인(`index.html`) 정품 푸터 레이아웃 및 8대 전문 정책 센터 뼈대 모달(`policy-center-modal`) 이식 완공 ➔ `[출판친구 정책·약관 >]` 및 `[개인정보처리방침 >]` 클릭 시 0.001초 만에 8-Tab 대형 모달창 격발 연동 + 사전 결제 예약 및 POD 맞춤 제작의 무결 법적 효력을 담은 서포터 이용약관 13개 조항 전문 & 5대 개인정보 처리방침 렌더링 수복 + 자바스크립트 제어 모듈(`openPolicyModal`) 파일 하단 정밀 주입 완공 + 'Wadiz-Style' 카피캣 뱃지 소거 및 '출판친구 정품' 뱃지 1차 브랜드 교정 완료 + 내일(8월 6일) 8대 약관 세부 내용 촘촘히 채우기 및 출판친구 럭셔리 고유 브랜드 UI 2차 고도화 개정 및 공지사항/제휴문의 전용 모달 전환 예정 사항 전역 동기화 보존 완료.
* **2026-07-24 (금)**: Supabase DB `reprint_candidates` 테이블 5대 원문 필드(`full_description`, `toc`, `review_list`, `authors_info`, `story_quotes`) 및 `cover_url` 확장 구축 + 25종 수집 단위를 20종 4열 5행 묶음(URL 500자 이하 및 API 차단 0%)으로 단권화 및 REST API페치·3단계 안전망(스피너 타임아웃, 소등, 2중 fallback 타이포 표지) 완공 + 살피미 수집 엔진의 500px 고화질 원작 실물 앞표지 자동 크롤링 & Supabase Storage `assets` 버킷 다이렉트 1:1 파일 적재 파이프라인 완공 (20/20 완벽 원작 표지 상용화) + Vercel 메인 라이브 배포 완공 + RAG 지식베이스 50개 조각 적재 동기화 완공 + 향후 살피미 가변 자동 수집 스케줄링(하루 3회 300종) 및 4계절 모달 5대 파트 연동 2종 실행계획서 수립 + 월 500만원 정산 글로벌 아마존(KDP) 및 국내 유통 EPUB3 무인 양산 1만종 프로젝트(한국어 학습 35종 + K-푸드 레시피 15종 공공 API 연동 지도) 신규 실행계획서(`실행계획서/2026-07-24_03_...md`) 작성 보존 완료.
